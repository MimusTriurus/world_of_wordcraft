// Доступ к ИИ-игроку из Node.
//
// САМОГО ИГРОКА ЗДЕСЬ НЕТ. Ядро решений живёт в word-shooter.html, в секции
// «автопилот»: бот — это режим игры, а не только измерительный прибор, и двух
// копий у него быть не должно. Разъехались бы они в первую же правку, и Node
// мерил бы не то, во что играет человек. Стенд и так поднимает настоящий скрипт
// из HTML, так что канонической копией выбрана игровая.
//
// Здесь остаётся только то, чего в игре быть не должно: границы классов и ручек
// наружу для свипов и калибровки. Числа и оговорки — docs/ai.md.
export { boot, mulberry32 } from "../sim/harness.mjs";

// Классы, ручки и политики читаются у игры, чтобы не разойтись с ней. Функции
// берут их у уже поднятого экземпляра: до boot() их не существует.
export const classesOf = g => g.BOT_CLASSES;
export const knobsOf = g => g.BOT_KNOBS;
export const policyOf = g => g.BOT_POLICY;

// Имена классов нужны гонялкам до первого забега, а поднимать игру ради трёх
// строк дорого — поэтому список продублирован, и стенд его проверяет.
export const CLASS_NAMES = ["быстрый", "средний", "медленный"];
